#you enter a number and then try to guess it, it will tell you "too high"
#or "too low" if your guess isn't there and of course it will tell you when you win

OUT "enter a number to guess"
INP ONE
LOOP A
OUT "enter a guess"
INP TWO
IF TWO > ONE OUT "too high"
IF TWO < ONE OUT "too low"
JMPNE ONE TWO A
OUT "you guessed it! It was: ONE"
END
